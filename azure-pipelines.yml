# MLB AI GO pipeline index
#
# This repo intentionally uses split Azure DevOps pipelines:
# - azure-pipelines-backend.yml deploys the .NET API to Azure Container Apps.
# - azure-pipelines-frontend.yml deploys the Angular app to Azure Static Web Apps.
#
# Keep this root entry disabled so a newly created pipeline does not deploy both
# sides by accident.

trigger: none
pr: none
